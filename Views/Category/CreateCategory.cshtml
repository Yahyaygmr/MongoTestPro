@model MongoTestPro.Dtos.CategoryDtos.CreateCategoryDto

@{
    Layout = "_AdminLayout";
    ViewData["Title"] = "Kategori Oluþtur";
}

<h1>Kategori Oluþtur</h1>

<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group my-3">
                <label asp-for="CategoryName" class="control-label">Kategori Adý</label>
                <input asp-for="CategoryName" class="form-control" />
                <span asp-validation-for="CategoryName" class="text-danger"></span>
            </div>
            <div class="form-group my-3">
                <input type="submit" value="Oluþtur" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="CategoryList">Listeye Dön</a>
</div>
